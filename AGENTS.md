Read CLAUDE.md for project architecture, conventions, and code quality standards.

Codex-specific overrides:
- Run `make test` to verify changes.
- This project uses Poetry. Run commands with `poetry run`.